{
  "name": "marinedieselengine",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/prebuild-check.mjs && astro build && node scripts/postbuild-check.mjs",
    "preview": "astro preview",
    "check": "python3 scripts/validate_content.py"
  },
  "dependencies": {
    "astro": "^5.6.1"
  }
}
